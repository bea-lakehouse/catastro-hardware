services:
  ml:
    build:
      context: .
      dockerfile: ml/Dockerfile
    container_name: catastro-ml
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
